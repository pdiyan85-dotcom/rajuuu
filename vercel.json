{
  "buildCommand": "pnpm --filter @workspace/mockup-sandbox run build && rm -rf dist && mv \"$(pnpm --filter @workspace/mockup-sandbox exec pwd)/dist\" dist",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
