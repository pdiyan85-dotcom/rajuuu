{
  "buildCommand": "pnpm --filter @workspace/mockup-sandbox run build && rm -rf dist && mv artifacts/mockup-sandbox/dist dist",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
